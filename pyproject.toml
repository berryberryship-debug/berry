[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "axioms-quantumlab"
version = "0.1.0"
description = "Architecture autopoïétique et moteur solitonique"
dependencies = [
    "numpy",
    "scipy"
]
